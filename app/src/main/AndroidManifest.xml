<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tiernoparla.educonex">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EduConex">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.EduConex"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividades adicionales -->
        <activity
            android:name=".ProfesorActivity"
            android:label="Actividad Profesor"
            android:exported="true">
        </activity>

        <activity
            android:name=".AlumnoActivity"
            android:label="Actividad Alumno"
            android:exported="true">
        </activity>

        <activity
            android:name=".AlumnosActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".EditarUsuarioActivity"
            android:label="Editar Usuario"
            android:exported="true">
        </activity>
        <activity
            android:name=".AsignaturasActivity"
            android:label="Asignaturas"
            android:exported="true">
        </activity>
        <activity
            android:name=".NotasActivity"
            android:label="Actividad de Notas"
            android:exported="true">
        </activity>



    </application>

</manifest>
